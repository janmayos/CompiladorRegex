#include <iostream>  
#include <stdlib.h> 
#include <iostream> 
#include "iostream" 
#include "string" 
using namespace std; //using namespace 
 
int Numero1 , Numero2 ; //comentar 

char Suma , Resta , Multiplicacion , Division, Operacion; 
 int hola(){
  return 0;
 }

int hola(int a ){
  return 0;
 }

int main() { 
  int a = -1;
  int a = -1,b=2;
  int a = -1,b;
  hola();
  hola(1);
  hola(2+2);

cout<<( " Ingrese Numero1 " ); 
cin>>(Numero1); 
cout<<( "Ingrese operacion" ); 
cin>>Operacion; 
   cout<<( "Ingrese Numero2" ); 
cin>>(Numero2); 
if(Operacion == '+') 
{ 
cout<<Numero1+Numero2; 
} 
if(Operacion == '-' ) 
 
{ 
cout<<Numero1-Numero2; 
} 
if((Operacion) == '*') 
{ 
cout<<Numero1*Numero2; 
} 
if(Operacion =='/')  
{ 
cout<<Numero1/Numero2 ; 
} 
cout<<( "Ingrese Numero2 "  ) << Numero1; 
std::cout<<( "Ingrese Numero2 "  ) << Numero1; 
 
 
if( (a == b && !aa != bb) || (aaa < bbb) || aba > abnab && kl <= ll || kss >= jkalskl) 
 
if( (a == b && !aa != bb)  ) 
if( a == b && !aa != bb  ) 
 
if( (a == b && !aa != bb) || (aaa < bbb) ) 
if( (a == b && !aa != bb) || aaa < bbb ) 
if( a == b && !aa != bb || aaa < bbb ) 
if( (a == b) && !aa != bb || aaa < bbb ) 
if( (a == b) && (!aa != bb || aaa < bbb )){ 
if( (a == b && !aa != bb && !aa != bb)  ) 
 
//a == b  
//aa != bb  
//aaa < bbb 
//aba > abnab  
//kl <= ll  
//kss >= jkalskl 
 
a++; 
b--; 
return 0; 
} 
 
for (int k = 0; k < 10; k++) 
for (int k = 0, m = 0; k < 10; k++, m--) 
for (;;) 
 
switch (x) 
{ 
 case 0:  
   cout << x << " is 0";  
   break; 
 case 1:  
   cout << x << " is 1"; 
    break; 
 default:  
 cout << x << " is not 1 or 2"; 
  break; 
} 
 
a = a+ba+b;
a = c-2+(a+b);
a = c+(a+b) ;
a = (c+(a+b)); 
 
a = (2*2)*(a+b); 
a = ((2/d)+(dl+2.2));
float a = 2;
float b = -222.22;
int jajajaj = a+ba+b+((2/d)+(dl+2.2)); 
char a = '2';
 